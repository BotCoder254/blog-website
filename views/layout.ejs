<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Website</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">
    <link href="/styles.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <nav class="bg-blue-600 p-4">
        <div class="container mx-auto flex justify-between items-center">
            <a href="/" class="text-white text-2xl font-bold"><i class="fas fa-blog mr-2"></i>Blog Website</a>
            <ul class="flex items-center">
                <% if(typeof user != 'undefined'){ %>
                    <li class="mr-4"><a href="/dashboard" class="text-white"><i class="fas fa-tachometer-alt mr-1"></i>Dashboard</a></li>
                    <li class="mr-4"><a href="/user/profile" class="text-white"><i class="fas fa-user mr-1"></i>Profile</a></li>
                    <li class="mr-4"><a href="/auth/logout" class="text-white"><i class="fas fa-sign-out-alt mr-1"></i>Logout</a></li>
                    <li>
                        <img src="../uploads/profile/ user.profileImage %>" alt="Profile" class="w-8 h-8 rounded-full">
                    </li>
                <% } else { %>
                    <li class="mr-4"><a href="/auth/login" class="text-white"><i class="fas fa-sign-in-alt mr-1"></i>Login</a></li>
                    <li><a href="/auth/register" class="text-white"><i class="fas fa-user-plus mr-1"></i>Register</a></li>
                <% } %>
            </ul>
        </div>
    </nav>

    <div class="container mx-auto mt-6">
        <%- body %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/alpinejs@2.8.2/dist/alpine.min.js" defer></script>
</body>
</html>